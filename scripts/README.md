## Local database scripts

This folder is intended for **local-only** SQL scripts (e.g. Railway MySQL setup).

All `*.sql` files under `scripts/` are **git-ignored** (see `.gitignore`) to avoid committing credentials.

